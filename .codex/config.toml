# Codex CLI — project configuration
#
# Instructions are NOT injected into this file. Codex reads AGENTS.md at the repo
# root, which scripts/sync_agents.py generates from promptkit/AGENTS.md. The
# Constitution therefore still reaches Codex — via the file Codex actually reads
# for instructions.
#
# This file is for Codex *settings* only and is no longer sync-managed.
#
# History: from v2.0.5 (75e69dd) through v2.0.7, sync_agents.py injected Markdown
# here, leaving the file unparseable as TOML ("Invalid statement at line 1"). No
# real settings were lost — the file had never held any. See CHANGELOG 2.1.0.
# sync_agents.py now refuses non-Markdown targets outright.
